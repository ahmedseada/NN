{
  "Adam": {"def": "Optimizer that keeps a running mean of each parameter's gradient and of its square, and steps by their ratio, so every parameter gets its own step size.", "hint": "Two moving averages plus a bias correction for the first steps.", "see": "Chapter 11"},
  "Autograd": {"def": "The mechanism that records every operation on tensors that require gradients (the tape) and replays it backwards on <code>Backward()</code> to compute all gradients.", "hint": "Reverse-mode application of the chain rule.", "see": "Chapter 4"},
  "Chain rule": {"def": "The rule for differentiating a composition: the derivative of an outer function applied to an inner one is the outer derivative times the inner derivative. Autograd applies it once per recorded operation.", "hint": "Rates of change multiply along a chain.", "see": "Pre-Calc volume <i>Numbers, Shapes, and Functions</i>, function composition; any calculus text for the derivative form"},
  "Device": {"def": "Where a tensor's numbers live and where operations on it run: <code>Device.Cpu</code> or <code>Device.Cuda(n)</code>. Operands of one operation must share a device.", "hint": "", "see": "Chapter 2"},
  "Epoch": {"def": "One full pass over the training data. Training usually runs many epochs.", "hint": "", "see": "Chapter 13"},
  "Gradient": {"def": "For every parameter, how much the loss changes when that parameter is nudged; the optimizer moves each parameter against its gradient.", "hint": "The vector of partial derivatives; its negative points downhill.", "see": "Chapter 4; Activation Functions volume for gradient effects of each activation"},
  "Loss": {"def": "A single number that measures how wrong the predictions are; training minimizes it.", "hint": "", "see": "Chapter 10"},
  "Module": {"def": "Anything that maps input tensors to output tensors and may own parameters: a layer, an activation, or a <code>Sequential</code> of them.", "hint": "", "see": "Chapter 5"},
  "MSE": {"def": "Mean squared error: the average of the squared differences between prediction and target. The default loss for regression.", "hint": "Squaring punishes large errors more than small ones.", "see": "Chapter 10"},
  "Optimizer": {"def": "The object that updates parameters from their gradients: <code>Sgd</code>, <code>Adam</code>, <code>AdamW</code>.", "hint": "", "see": "Chapter 11"},
  "Parameter": {"def": "A tensor the network learns (weights and biases). Created with gradient tracking on and exposed through <code>Module.Parameters()</code>.", "hint": "", "see": "Chapter 5"},
  "ReLU": {"def": "Activation that keeps positive inputs and replaces negative ones with zero. Cheap and the usual default in deep networks; a unit whose input stays negative stops learning.", "hint": "Gradient is 1 for positive inputs and 0 otherwise.", "see": "Activation Functions volume, ReLU"},
  "Sigmoid": {"def": "Activation that squashes any number into the range 0 to 1; used for probabilities of yes/no outputs.", "hint": "S-shaped curve; its slope is largest at 0 and vanishes far from it.", "see": "Activation Functions volume, Sigmoid; Pre-Calc volume, exponential functions"},
  "Tanh": {"def": "Activation that squashes any number into the range −1 to 1; zero-centred, a good default for small networks.", "hint": "A rescaled and shifted sigmoid.", "see": "Activation Functions volume, Tanh"},
  "Tensor": {"def": "An n-dimensional array of 32-bit floats with a shape and a device. The basic data type of the library.", "hint": "", "see": "Chapter 3"},
  "TensorScope": {"def": "A <code>using</code> block that disposes every tensor created inside it when it ends, returning device memory to the pool.", "hint": "", "see": "Chapter 3"},
  "XOR": {"def": "Exclusive or: the output is 1 when exactly one of two inputs is 1. The smallest problem a network without a hidden layer cannot solve.", "hint": "The two classes cannot be split by one straight line.", "see": "Activation Functions volume, XOR impossibility chapter"}
}
